# Jenkins CLI Wrapper (`jenkinsctl`)

A standalone Go-based CLI tool to interact with Jenkins API.

## Usage
For detailed usage instructions, refer to the [Jenkins CLI Skill Documentation](../../skills/jenkinsctl/SKILL.md).
